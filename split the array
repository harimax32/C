#include <stdbool.h>

bool isPossibleToSplit(int* nums, int numsSize) {
    int count[101] = {0};
    
    for (int i = 0; i < numsSize; i++) {
        count[nums[i]]++;
        if (count[nums[i]] > 2) {
            return false;
        }
    }
    
    return true;
}
